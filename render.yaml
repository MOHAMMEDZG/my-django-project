services:
  - type: web
    name: lessons-site
    env: python
    region: frankfurt
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn lessons_site.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11